# Me-adi Frontend

Static frontend for Vercel.

Before deploying, update the `api-base-url` meta tag in `index.html` to the public Render backend URL, for example `https://your-service.onrender.com`.

Deploy this repository with Vercel using the repository root as the project root. No build command is required.
